# Minimal deps for Streamlit Community Cloud.
# Versions pinned to known-stable ranges (pandas 2.x; plotly 5.x).
# The cloud dashboard reads pre-committed CSVs from results/; it does NOT
# need finance-datareader / pykrx at runtime — those live in requirements-full.txt.

streamlit>=1.30,<2.0
plotly>=5.18,<6.0
pandas>=2.0,<3.0
numpy>=1.24,<2.3
