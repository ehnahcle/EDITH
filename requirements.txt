# Minimal deps for the Streamlit Cloud deployment.
# The cloud dashboard reads pre-committed CSVs (results/*.csv);
# it does NOT need FinanceDataReader / pykrx at runtime.
# For local development or CI cron, install requirements-full.txt instead.

streamlit>=1.30
plotly>=5.18
pandas>=2.0
numpy>=1.24
