# Streamlit secrets example
# Copy to .streamlit/secrets.toml (locally) and add the same in
# Streamlit Cloud → App settings → Secrets.
# secrets.toml is git-ignored — never commit real passwords.

# Optional password lock for the dashboard.
# Leave empty or omit to make the dashboard open to anyone with the URL.
password = "change-me-to-something-strong"
